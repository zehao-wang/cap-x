"""Long-term **skill** library (self-evolve fixed memory).

General functions composed from primitives (``docs-se/storage.md``). One promoted
``*.py`` per skill; empty until the self-evolve loop固化s something here. Exposed
to the agent via ``capx.self_evolve.long_term_library`` (prompt docs + namespace
injection), not by importing this package directly.
"""
